# Examples

Integration examples for consuming SDKWork Canvas from other applications.

- Use `@sdkwork/canvas-app-sdk` composed facade for app-api calls.
- Use `@sdkwork/drive-app-sdk` for all file uploads.

Authority: `../sdkwork-specs/APP_SDK_INTEGRATION_SPEC.md`
