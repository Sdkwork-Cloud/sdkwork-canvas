# sdkwork-canvas-pc-desktop

Tauri native host for SDKWork Canvas PC desktop and tablet targets.

This README is the SDKWork module entrypoint for `sdkwork-canvas-pc-desktop`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../../sdkwork-specs/`.
